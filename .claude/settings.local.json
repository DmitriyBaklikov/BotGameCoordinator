{
  "permissions": {
    "allow": [
      "Bash(bundle exec rails db:migrate)",
      "Bash(bundle exec rails runner \"ActiveSupport::TimeZone.all.map { |tz| [tz.name, tz.utc_offset / 3600.0] }.uniq { |_, o| o }.sort_by { |_, o| o }.each { |name, off| puts \"\"#{name} => UTC#{off >= 0 ? ''+'' : ''''}#{off % 1 == 0 ? off.to_i : off}\"\" }\")",
      "Bash(bundle exec rails db:rollback)",
      "Bash(bundle exec rspec spec/services/telegram_calendar_spec.rb spec/models/game_spec.rb)",
      "Bash(bundle exec rspec)"
    ]
  }
}
